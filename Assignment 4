#create dictionary: key - symbol, value - code
with open("new.txt","r") as t:
    txt1 = t.read()
print(txt1)



if(d[1].startswith(symbol)):
            symbols+=d[0]
        if d[1] == symbol:
            b+=1
            a.append(d[0])
            print(symbol,':',symbols)
            symbol =""
            print()
            print('Step',b)
            break
